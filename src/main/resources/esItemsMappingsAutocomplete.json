{
    "items": {
        "properties": {
            "nameAutoComplete": {
				"analyzer": "autocomplete",
				"type": "string"
            },
            "aliasAutoComplete": {
				"analyzer": "autocomplete",
				"type": "string"
            }
        }
    }
}